# Copy to .env and adjust if needed. Never commit the real .env (see .gitignore).

DATABASE_URL="postgresql://golfstockmarket:localdev@localhost:5432/golfstockmarket"
REDIS_URL="redis://localhost:6379"
